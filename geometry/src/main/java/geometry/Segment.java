package geometry;

public final class Segment extends Form implements Mesurable1D{
    @Override
    public void translate(double deltaX, double deltaY) {

    }

    @Override
    public double length() {
        return 0;
    }
}
